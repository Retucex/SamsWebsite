@model BlogPost
@{
    ViewData["Title"] = $"SLIInc - {@Model.Title}";
}

<h2>@Model.Title</h2>
<h4>@Model.Published</h4>
<p>@Model.Content</p>

@foreach (var comment in @Model.Comments)
{
    <h5>@comment.Author</h5>
    <p>@comment.Comment</p>
}